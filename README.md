Reads a file containing a list of processes to schedule and simulates the execution of those processes using three different scheduling algorithms.

To run:
    make build
    ./Scheduler.out <input file>